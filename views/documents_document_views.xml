<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="action_documents_request_session_signature" model="ir.actions.server">
        <field name="name">Pedir firma de la sesión al tutor</field>
        <field name="model_id" ref="documents.model_documents_document"/>
        <field name="binding_model_id" ref="documents.model_documents_document"/>
        <field name="binding_view_types">list,kanban</field>
        <field name="state">code</field>
        <field name="code">action = records.action_cs_request_session_signature()</field>
    </record>

</odoo>
